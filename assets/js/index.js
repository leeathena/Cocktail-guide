// Function to search for cocktails
function searchCocktails(searchTerm) {
    // API call to search for cocktails by name or ingredient
    // Process and display the best matched recipe
    // Store searched term to local storage
    // Update sidebar with the searched term
    // Display the best matched recipe based on the search term
}

// Function to store searched terms in local storage
function storeSearchTerm(searchTerm) {
    // Save the searchTerm to local storage
}

// Function to display searched terms in the sidebar
function displaySearchTermInSidebar(searchTerm) {
    // Update the sidebar with the latest searched term
    // Allow users to click the search terms and see recipes (button function perhaps)
    // obtain search history from local storage (get item)
}

// Function to display searched recipe
function displaySearchedRecipe(recipeData) {
    // Clear existing content in the display area (.empty)
    // Include recipe's name, ingredients, and instructions
    // Optionally, include options for the user to favorite or save the recipe
}

// Function to fetch and display the top 10 popular recipes
function displayPopularRecipes() {
    // API call to fetch top 10 popular recipes when 'popular recipes' button is clicked (event listener)
    // Display these recipes (present in a list of hyperlinked recipe names)
}

// Function to fetch and display a random recipe
function displayRandomRecipe() {
    // API call to fetch a random recipe when 'random recipes' button is clicked (event listener)
    // Display the random recipe
}

// Function to translate text using the translation API
function translateText(text, targetLanguage) {
    // API call to translate text based on user's chosen language
    // Display translated text
}